Make sure to do the following commands:

Install Node.js

run: npm init -y

run: npm install discord.js

run: npm install shelljs

run: npm install concurrently --save ?????

get config.json file from desktop
